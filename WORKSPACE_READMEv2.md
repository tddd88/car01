# 智能导航避障与视觉识别机器人 - 实车开发文档 (硬件优先版)

> **当前阶段核心目标**：暂缓上层状态机与复杂决策逻辑。全力攻克实车底层通讯、传感器融合（打滑处理）以及稳定建图。做到“指哪打哪，建图不飘”。
> **硬件平台**：树莓派 4B 8GB + 2D激光雷达 + STM32底盘控制板（麦轮+铅酸）+ IMU
> **软件环境**：Ubuntu 20.04 + ROS Noetic + C++ 

## 1. 当前精简架构 (实车基础版)

| 模块层级 | 功能包/包名 | 节点名称 | 核心功能描述 | 订阅话题 | 发布话题 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **底层通信** | `robot_bringup` | `stm32_bridge` | **当前最高优先级**。串口通讯解析，双向传输底盘控制流与传感器状态 | `/cmd_vel` | `/odom_raw`, `/imu/data` |
| **硬件驱动** | `robot_bringup` | `lidar_driver` | 驱动2D激光雷达，调节滤波参数 | - | `/scan` |
| **多源融合** | `robot_localization`| `ekf_se` | **核心难点**。融合轮式里程计与IMU，输出高精度里程计，应对麦轮打滑 | `/odom_raw`, `/imu/data` | `/odom` |
| **基础感知** | `slam_gmapping` | `gmapping` | 基于高精度 `/odom` 和 `/scan` 进行实车建图测试 | `/scan`, `/tf` | `/map` |
| **基础控制** | `teleop_twist_keyboard` | `teleop` | 键盘遥控底盘，验证正逆运动学与通讯延迟 | - | `/cmd_vel` |

---

## 2. 开发路线图 (Timeline: 基础硬件攻坚)

### Phase 1: 树莓派与STM32串口打通 (当前进度)
* [ ] **协议制定**：与32队友确定简单的二进制串口协议（帧头+数据位+校验和+帧尾）。
* [ ] **ROS下发测试**：编写 `stm32_bridge.cpp`，订阅键盘的 `/cmd_vel`，解析并打包发给32。实测车轮能否按预期正反转。
* [ ] **ROS接收测试**：32计算好实时的 $v_x, v_y, v_{th}$ 和 IMU 原始数据发给 ROS，`stm32_bridge` 解析并发布 `nav_msgs/Odometry` (无协方差版) 和 `sensor_msgs/Imu`。

### Phase 2: 里程计闭环与 EKF 融合防滑 (核心调优)
* [ ] **单里程计验证**：只跑 `stm32_bridge`，在 Rviz 中观察 `/odom_raw` 对应的 TF。遥控车子走一米，看 Rviz 里是不是也走了一米；原地转一圈，看 Rviz 里是否准确回到原角度（极大概率会偏，因为打滑）。
* [ ] **引入 EKF**：配置 `robot_localization`。将 IMU 的角速度和线加速度与轮式里程计融合。
* [ ] **融合调优**：不断调整 EKF 配置文件中的协方差矩阵权重。目标是：**在物理瓷砖上原地快速旋转+急刹车，Rviz 中的车头朝向不发生漂移。**

### Phase 3: 雷达上车与稳定建图
* [ ] **静态TF发布**：在 Launch 文件中写死 `base_link` 到 `laser` 的 `static_transform_publisher`。
* [ ] **建图测试**：启动雷达和 Gmapping，遥控底盘建图。如果 Phase 2 做的足够好，此时建出的走廊/房间地图边缘应该是笔直的，不会出现重影或地图撕裂。

### Phase 4: 上层逻辑预研 (待比赛规则明确后启动)
* [ ] **记录坐标点方案**：测试在建图时通过 `rviz` 或脚本记录绿纸的 `(x,y)` 坐标，直接将该坐标作为 `move_base` 的目标点。
* [ ] **视觉伺服节点**：测试单目相机的绿色识别阈值。
* [ ] **决策状态机**：根据最终版规则，组合上述功能，完成全流程自动化。

---

## 3. 串口协议设计建议 (给 32 队友的参考)

为了保证通讯稳定，建议采用定长或带有严格校验的二进制帧，不要用字符串（如 JSON 或纯文本的 printf），太占 CPU 且解析慢。

**下发指令 (ROS -> STM32) [例: 11字节]**
`[0xAA] [0x55] [速度指令 0x01] [vx_H] [vx_L] [vy_H] [vy_L] [vth_H] [vth_L] [CRC8] [0xEE]`
*(注：浮点数可以乘1000转成int16发送)*

**上传数据 (STM32 -> ROS) [例: 25字节]**
`[0xAA] [0x55] [数据指令 0x02] [vx...] [vy...] [vth...] [acc_x...] [acc_y...] [acc_z...] [gyro_z...] [CRC8] [0xEE]`