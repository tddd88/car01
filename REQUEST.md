# 需求

规则：

智能机器人避开场地内随机设置的障碍物，以最快的速度自主导航到达终点。需要参赛选手设计导航算法避开障碍物，并利用视觉算法识别终点的停止标志（绿色的A4纸），并停止在终点区域内，终点区域为边长0.8m的方形区域。比赛场地示意图如下所示。参赛机器人体积不能过大，原则上应控制在50cm*50cm*50cm以内，要求机器人为自主搭建的智能机器人，其运动结构可采用轮式、履带形式、万向轮形式等其他形式，采用相机感知终点，可选用激光雷达、超声波雷达、双目相机、深度相机、IMU等传感器辅助感知。为鼓励自主开发机器人进行比赛，若有两队机器人的关键机构相似度极高，则认为是雷同机器人，其中优胜者正常参加排名，其余雷同机器人排在所有其他队伍之后。关键机构包括：轮间距、电机型号、轮子直径等，其中任意两项有雷同则认为是雷同机器人。

1、 在比赛日的前一天，通过抽签，决定参赛队伍的比赛顺序。

2、 每支参赛队伍共有20分钟时间来完成包括调试、竞赛场地建图、导航任务以及奖励任务，其中每支参赛队最多有三次机会完成导航任务，选择最优成绩作为最终成绩。

3、 参赛队伍准备好后，由裁判发令开始计时，若裁判认定机器人成功到达终点，则时间成绩有效；若参赛队长向裁判示意终止本次导航，或机器人在5分钟内无法到达终点，则本次任务失败，记录机器人几何中心点与终点区域中心点的欧式距离作为距离成绩。宣布计时开始后选手仍然控制机器人、机器人撞倒障碍物或者机器人撞坏比赛场地直接判定为任务失败。

4、 参赛队伍准备好后，由裁判发令开始计时，同时参赛队伍需要一键启动车辆。（例如：物理按键、键盘空格键、键盘回车键，但禁止使用Rviz工具的2D Nav Goal 和发送固定目标点的程序来发送目标点）

5、 机器人到达终点的标准：机器人2/3投影面积位于终点区域内且至少保持3秒不动，则认为机器人到达终点，其余情况一律认为未到达终点。其中停止时间不计入导航时间，仅作为完成导航任务的标准。

6、 除了机器人导航到终点的时间外，本竞赛加入了时间惩罚机制来计算参赛队伍的时间成绩，具体的计算公式和计分细则如下：

7、 本竞赛优先根据参赛队伍的时间成绩进行排名，时间越短，排名越高。得到时间排名后，再根据距离成绩对没有时间成绩的参赛队伍进行排名，距离越短，排名越高。

8、 本竞赛鼓励参赛选手以激光雷达或深度相机建图、导航的方式完成比赛，参赛前，所有机器人必须通过组委会检查，各队伍和裁判共同指定机器人的几何中心点用于距离测量。禁止机器人本体与外界有数据交互，严禁遥控。组委会会提前一周向参赛队伍开放比赛调试场地，各参赛队伍可自行调试，为保证比赛挑战性，正式比赛中环境会进行适当变动。

9、 为鼓励自制机器人与自主开发算法，设特等奖一项。在竞速赛的前3名中，若参赛队的软硬件自主开发度较高，机械部分自主设计与制作，且导航代码有一定创新性（赛后提交创新性说明文档与导航源码作为证明），经择优答辩评选，评为特等奖，若前3名均不符合条件，则本届特等奖空置。

下面给出**最终版车载全栈架构（C++节点）** 和**开发流程**。

---

## 一、最终架构（树莓派端）

所有节点运行在树莓派4B 8GB，Ubuntu 20.04 + ROS Noetic，C++编写。

### 功能包与节点分布

| 功能包 | 节点名称 | 文件 | 功能 | 发布话题 | 订阅话题 |
| --- | --- | --- | --- | --- | --- |
| **`robot_bringup`** | `lidar_node` | `lidar_node.cpp` | 驱动激光雷达 | `/scan` (sensor_msgs/LaserScan) | 无 |
|  | `camera_node` | `camera_node.cpp` | 驱动USB摄像头 | `/rgb/image_raw` (sensor_msgs/Image) | 无 |
|  | `stm32_bridge` | `stm32_bridge.cpp` | 串口通信 | `/odom` (nav_msgs/Odometry) | `/cmd_vel` |
| **`slam_navigation`** | `slam_gmapping` | 标准gmapping | 建图（手动遥控时） | `/map` | `/scan`, `/odom`, `/tf` |
|  | `amcl` | 标准amcl | 定位（比赛时） | `/amcl_pose` | `/scan`, `/map`, `/odom`, `/tf` |
|  | `move_base` | 标准move_base | 路径规划 | `/cmd_vel` | `/scan`, `/map`, `/odom`, `/tf` |
| **`vision_control`** | `color_detector` | `color_detector.cpp` | 检测绿纸，发布停止标志 | `/stop_flag` (std_msgs/Bool) | `/rgb/image_raw` |
| **`decision`** | `race_state_machine` | `race_state_machine.cpp` | 状态机：等待启动 → 导航到approach点 → 搜索 → 停车 | 调用`/move_base` action，订阅`/stop_flag` | 无 |

**说明**：

- 无物理按键节点，一键启动通过终端执行`roslaunch`命令实现。
- `stm32_bridge`节点前期可以写一个模拟版本（发布虚拟`/odom`，订阅`/cmd_vel`打印日志），待STM32就绪后再替换为真实串口通信。
- `race_state_machine`启动后默认进入IDLE状态，需要外部触发（例如订阅一个`/start`话题，或者直接在节点启动后延时几秒自动开始）。为了“一键启动”，我们可以在launch文件中让状态机节点启动后自动开始（例如等待1秒后自动进入NAVIGATE状态），这样运行`roslaunch`命令就等于一键启动。

上面是规则和目前的架构；

我的一些问题：

1.比赛前是有建图但是建图那激光雷达绿纸在哪（识别不了颜色）阿，怎么让机器人往绿纸方向运动，给我几个思路吧，规则禁止使用Rviz工具的2D Nav Goal 和发送固定目标点的程序来发送目标点；这个寻址逻辑是不之后搞，在决策功能包里的，正常要后写的吧；

2.停止信号应该好弄吧，是不是到时候调试时候设置好绿色的占比就行了；

3.启动逻辑就写个大launch把所有都启动好就行了，到时候直接回车；最后直接最上层调用的launch是不搞两个，一个见图遥控保存地图，一个调用地图去自主导航

4.32bridge也不着急吧反正在决策包之前搞了就行，也看32的进度吧，比较灵活，前后都行，可以先写导航和视觉和其他的，实车还没出来，我先PC上搞个工作空间跑跑仿真呗还是，开发主要还是先在树梅派上吧，仿真到时候就那一大堆东西复制到PC，参数调差不多再移植回去对吗，告诉我什么时候/哪些东西需要跑仿真，哪些是直接拿真车直接跑的（很快也就会有实车）；

5.架构这样可以吗（毕竟也是我给想法然后AI生成的）

6.我们要做麦轮的，机械/布局的时候提醒队友（机械+32）注意重心尽量在中间了，暑假看打滑情况（比赛场地是室内瓷砖，我们车子不重不轻吧，要用铅酸）要不要加IMU，32也有引脚，我这边还是32那在逻辑上当odom突变（打滑时候多给IMU一些权重（惯导这东西也不会打滑，挺准的的吧）

7.现在4月中旬，12月末比赛；机械模型，仿真用简化URDF，单电机闭环PID，树梅派环境配置（之前我调了半个月的旭日X3和雷达和D435,但是架构改完我就改到raspi了，（之前我调了半个月的旭日X3和雷达和D435,但是架构改完我就改到raspi了，摄像头也就用USB的了）